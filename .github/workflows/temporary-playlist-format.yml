name: Temporary playlist formatter

on:
  push:
    branches: [feature/visual-playlist-library-v2]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Format playlist library files
        run: |
          npx prettier --write \
            apps/dashboard/src/App.tsx \
            apps/dashboard/src/pages/PlaylistLibraryPage.tsx \
            apps/dashboard/src/pages/PlaylistLibraryPage.test.ts \
            apps/dashboard/src/pages/PlaylistLibraryPage.css
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/dashboard/src/App.tsx \
            apps/dashboard/src/pages/PlaylistLibraryPage.tsx \
            apps/dashboard/src/pages/PlaylistLibraryPage.test.ts \
            apps/dashboard/src/pages/PlaylistLibraryPage.css
          if ! git diff --cached --quiet; then
            git commit -m "Format playlist library"
            git push
          fi
