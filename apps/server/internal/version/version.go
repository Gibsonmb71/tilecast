// Package version exposes the Tilecast server release version used in API
// responses and backup manifests.
package version

// Version is the Tilecast server release version.
const Version = "0.10.0"
