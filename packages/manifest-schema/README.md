# Manifest schema

`schema-v1.json` is the original direct-assignment single-zone contract. `schema-v2.json` adds an explicit direct fallback playlist, relevant schedule rules, all required playlists, server time, and preparation policy. Go and Kotlin models map to these transport schemas rather than database rows. Released versions are not reinterpreted.
